# Real .NET Projects For C# Exercises

C# exercises will run inside real generated .NET project workspaces rather than single-file script-style snippets. This supports full Monaco IntelliSense through project-aware language services, realistic test execution, package references, namespaces, and project structure, while the UI can still present a simplified file tree for learners.

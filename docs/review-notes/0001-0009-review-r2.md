# Re-Review (Revision 2): Tasks 0001–0009 — After Fixes

**Review date:** 2026-06-14
**Context:** Follow-up to [0001-0009-review.md](./0001-0009-review.md). The author addressed the prior findings; this pass re-verifies each previously-flagged gap against the **current** code and re-states the bar. Method unchanged: read real code/tests, verify each criterion, trust nothing on claim alone.

Legend: ✅ Met · ⚠️ Partial · ❌ Missing. Per-gap status: **FIXED** / **PARTIALLY FIXED** / **STILL BROKEN**.

---

## Executive summary — what changed

| Task | Prior status | Now | Movement |
|---|---|---|---|
| 0001 Scaffold | ✅ Accurate | ✅ Accurate | No change needed. |
| 0002 App shell | ⚠️ Over-marked | ⚠️ Closer, not done | **Keyboard shortcuts FIXED**, **per-profile theme FIXED**, **frontend tests FIXED**. Still: no explicit light ruleset, palette still hardcoded. |
| 0003 Content loading | ⚠️ | ⚠️ | **No movement on the 3 flagged gaps.** Idempotency still delete-all + untested; `exercise.yml kind` still unparsed; still no broken-content tests. |
| 0004 Curriculum path | ⚠️ | ⚠️ | **No movement on the 3 flagged gaps.** Still hand-rolled markdown; review/project sections still raw headings; Dashboard mastery still hardcoded. |
| 0005 Sources/notes/search | ⚠️ | ⚠️→✅-ish | **Concept page + notes FIXED**, **deep-links FIXED**, **search now real SQL**. Still: no markdown-body search; thin note-target tests. |
| 0006 Diagnostic + soft locks | ⚠️ | ⚠️ | **Milestone soft-lock FIXED**, **soft-lock tests FIXED**. Locks now order-driven (not hardcoded) but still not authored-prerequisite-driven; `.md` prerequisite text ignored. |
| 0007 Monaco | 🟡 In progress | 🟡 In progress | **Hover FIXED**, **sig-help/code-action wiring FIXED**, **SetupMessages FIXED**, **.editorconfig emitted**. Still: code actions hand-rolled (not Roslyn CodeFix/Refactoring); analyzers never run; no workspace caching. |
| 0008 Workspaces | ❌ Mislabeled | ⚠️ Much improved | **DATA-LOSS cleanup FIXED**, real csproj/sln FIXED, content-sourced files FIXED. New issues: run dirs leak forever; idempotency breaks on content edits; editing workspace not per-attempt. |
| 0009 Runner | ❌ Mislabeled | ✅ Defensible | **SECURITY leak FIXED (with a failing-hidden test)**, **runner abstraction FIXED**, **RunnerError state FIXED**. Residual: no sandbox impl; run-dir cleanup. |

**Headline:** The two most serious prior defects are resolved — **0009's hidden-test source leak (security)** and **0008's recursive cleanup (data-loss)** are both genuinely fixed and, for the leak, covered by a dedicated failing-hidden test. **0002**, **0005**, and **0006** moved materially forward. **0003**, **0004**, and the "real Roslyn" core of **0007** did not move and remain the open work.

---

## TASK 0001 — Scaffold .NET 10 + React

All seven criteria ✅ Met (unchanged): `src/`+`tests/` layout (`ProdigeeTutsPoint.slnx:2-10`), `net10.0` (`Directory.Build.props:3`), React 19/TS/Vite (`Web/package.json:17-37`), EF Core Sqlite (`Directory.Packages.props:13-15`, `DependencyInjection.cs:21`), centralized settings (`Directory.Packages.props:3`), README dev-server docs (`README.md:7-18`).

**Verdict:** ✅ "Completed" defensible. Only caveat (as before): a fresh `dotnet build`/`npm run build` was not executed this pass — verified by inspection.

---

## TASK 0002 — Build the local app shell

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Explicit routes for all areas | ⚠️ | `App.tsx:92-114` covers all categories (now incl. `/concepts/:conceptId`), but projects/lessons/exercises have **detail routes only** — no top-level list routes; "Projects" nav hardcodes `/projects/wordfreq-csharp` (`AppShell.tsx:47`). |
| Default profile, no login | ✅ | `App.tsx:27-39`. |
| Light/dark/neutral themes | ⚠️ **STILL BROKEN** | `index.css` has `:root`, `[data-theme='neutral']` (:37), `[data-theme='dark']` (:60) — still **no `[data-theme='light']` ruleset**; light renders via base `:root` fallback. |
| Theme persists per profile | ✅ **FIXED** | `App.tsx:51` writes `profileThemeKey(profile.id)` = `prodigee.theme.<id>` (`:130-132`); legacy global fallback on read (`:41`); test asserts `prodigee.theme.default-profile` (`App.test.tsx:43`). |
| Command palette opens all categories | ⚠️ **STILL BROKEN** | `AppShell.tsx:26-42` `commandItems` is still a **hardcoded static array** with single sample entries; not driven by the content graph/API. |
| Keyboard shortcuts K / Enter / S / Esc | ✅ **FIXED** | `App.tsx:55-76`: K opens palette, **S → `prodigee:save`** (:63-65), **Enter → `prodigee:run`** (:68-71), Esc closes. Consumed end-to-end in `ExerciseDetail.tsx:92-106` (`saveActiveFile()`/`runExercise()`). All 4 now real (was 2/4). |
| React assets servable by ASP.NET Core | ✅ | `Program.cs:17,26-32,40-52`. |
| Frontend tests exist | ✅ **FIXED** | `package.json:10,37` `vitest run` + Testing Library + jsdom; `vite.config.ts:12-15`; `App.test.tsx` (theme persistence + save/run shortcuts), `workspaceFiles.test.ts`. Was ZERO. |

**Remaining gaps:** No explicit `light` theme token set; command palette still a hardcoded demo list; no top-level list routes; `SettingsPage.tsx:14-19` still a thin stub; tests don't cover routing/palette.

**Verdict:** ⚠️ The two hard fails (shortcuts, per-profile theme) plus the zero-tests gap are genuinely fixed. **"Completed" still not fully defensible** — explicit light theme and dynamic palette criteria remain unmet as written. Much closer.

---

## TASK 0003 — Implement hybrid content loading

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Folder structure / parse / index all types | ✅ | `ContentIndexingService.cs:58-217`, doc classes `:387-509`. |
| Stable ids + content version | ✅ | YAML ids verbatim; `ContentVersion` propagated `:119,142,163,175`. |
| Broken content → diagnostics (code path) | ✅ (code) | `Validate` `:255-296`, `ContentIndexingException` `:51`. |
| Idempotent without duplicates | ⚠️ **STILL BROKEN (untested)** | Still `ClearIndexedContentAsync` `ExecuteDeleteAsync` of every table then re-insert `:56,219-234` (now in a transaction `:54,74` — minor). Not upsert; **no idempotency test** (`ContentIndexingService` referenced in zero test files). |
| Per-exercise `exercise.yml` `kind` parsed | ❌ **STILL BROKEN** | `exercise.yml` has `kind: focused`, but indexing reads only `track.yml`; `ExerciseContentDefinition` exposes only `Workspace`; `Exercise` entity has no `Kind`. `grep -i kind` over Domain+Infrastructure → nothing. Silently dropped. |
| Broken-content negative tests | ❌ **STILL BROKEN** | No test constructs broken content / asserts diagnostics. The entire `Validate`/exception path is unverified. |

**Verdict:** ⚠️ Core indexing solid, but **none of the three flagged gaps moved**. **"Completed" not defensible.**

---

## TASK 0004 — Render the first curriculum path

| Criterion | Verdict | Evidence / status |
|---|---|---|
| C# track page from indexed content | ✅ | `TrackDetail.tsx:8-31` → `CurriculumEndpoints.cs:28-54`; test `:9-21`. |
| wordfreq-csharp project page | ✅ | `ProjectDetail.tsx:10-39` → `:191-212`. |
| Milestone 1 project-backed mastery flow | ❌ **STILL BROKEN** | Milestone page shows lessons/exercises/markdown only; no progress/mastery state; no per-milestone progress endpoint. |
| Lesson reader rich sections/code/review prompts/project connection | ⚠️ **PARTIALLY (no movement)** | Renders markdown + naive C# highlight (`MarkdownText.tsx:34-79`), but review prompts/project connection are STILL plain `##` headings (`text-as-data-csharp.md:26-34`); `LessonDetailResponse` returns only raw `Markdown` (`CurriculumEndpoints.cs:494-501`). |
| Milestone lists required lessons + exercises | ✅ | `MilestoneDetail.tsx:25-30`; test asserts 6+10. |
| Soft-lock warnings, non-blocking | ✅ | `SoftLockNotice.tsx:5-34`. |

**Re-checked gaps:** real markdown lib — ❌ STILL BROKEN (no marked/remark/markdown-it in `package.json`; hand-rolled `MarkdownText.tsx:115-206` drops ordered lists/blockquotes/tables/links/bold/italic). Structured review/project sections — ❌ STILL BROKEN. Dashboard real mastery — ❌ STILL BROKEN (`Dashboard.tsx:27-30` hardcodes `"0/1"`,`"0/10"`,`"Introduced"`,`"0"`; never calls the existing `/mastery/concepts` or `/diagnostics/.../latest`).

**Verdict:** ⚠️ Pages render and soft-locks display, but **all three flagged gaps remain unfixed**. **"Completed" not defensible.**

---

## TASK 0005 — Add source references, notes, search

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Index books/refs from YAML | ✅ | `ContentIndexingService.cs:58-66,198-217`. |
| Refs as metadata pointers, not content | ✅ | `SourceReferenceResponse` chapter/pages/topic/usage only (`CurriculumEndpoints.cs:520-527`); test `:131`. |
| Notes on all 6 target types | ✅ | `TargetExistsAsync` validates all six `LearningEndpoints.cs:281-291`. |
| Concept page/route + concept-note UI | ✅ **FIXED** | `ConceptDetail.tsx:23` `NotesPanel targetType="concept"`; route `App.tsx:102`; `GET /concepts/{id}` `:177-189`. (Was API-only.) |
| Notes in SQLite | ✅ | upsert `:103-148`. |
| Search covers all 6 types | ✅ | `:94-161`. |
| Search is a real DB query | ⚠️ **PARTIALLY FIXED** | Now real SQL via `EF.Functions.Like` w/ escaping (`:92-161,356-362`) — in-memory scan fixed. **But still does NOT search markdown body** (only Title/Summary/Description/Topic/Usage/Chapter). |
| Concept/SourceRef focused deep-links | ✅ **FIXED** | Concept → `/concepts/{id}` (`:145`); SourceRef → `/sources#{id}` (`:159`) with matching anchors (`Sources.tsx:24`). |
| Broader tests | ⚠️ **PARTIALLY FIXED** | Notes now tested for concept + source-ref (`LearningEndpointTests.cs:36-64`); search has concept deep-link test (`CurriculumEndpointTests.cs:41-68`). Still no project/milestone/exercise note tests, no BadRequest-target test, no body-search test. |

**Verdict:** ✅ Concept page/notes and deep-links genuinely fixed; search upgraded to real SQL. ⚠️ Markdown-body search still unmet; note-target tests still thin. **"Completed" defensible if search is scoped to metadata fields; not if body search was in scope.**

---

## TASK 0006 — Light diagnostic and soft locks

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Diagnostic covers all 6 areas | ✅ | 6 questions `LearningEndpoints.cs:10-72`; test `:78`. |
| Attempts stored per profile | ✅ | `:185-199`, `/latest` `:156-173`. |
| Recommend primer/drill/milestone | ✅ | `:250-268`; test `:101`. |
| Soft locks on lessons/exercises/milestones | ⚠️ **PARTIALLY FIXED** | Now derived (`CurriculumEndpoints.cs:364-442`) from positional `Order`, not declared prerequisites. |
| Learner can continue | ✅ | `SoftLockNotice.tsx:23`. |
| Diagnostic feeds concept mastery | ✅ | `:201-217`, aggregate `:232-245`; test `:103-108`. |

**Re-checked gaps:** Prerequisite-driven? — ⚠️ PARTIALLY: no longer hardcoded, but **no declared prerequisites exist** (no `Prerequisite` field in domain, none in `track.yml`); locks are purely positional (`Order`); the `## Soft-lock prerequisites` markdown in `pure-word-counting-core.md:31-36` is dead text, never parsed. Milestone soft-lock semantically wrong? — ✅ **FIXED** (now returns prior milestones only; milestone 1 → empty). Zero soft-lock tests? — ✅ **FIXED** (`CurriculumEndpointTests.cs:86-113` `SoftLocksAreDataDrivenFromCurriculumOrder`, passes).

**Remaining gaps:** Order-based, not authored-prerequisite-based; authored prerequisite markdown ignored (content/behavior mismatch). Exercise lock uses `<=` so it can include concurrent lessons.

**Verdict:** ⚠️ Semantically-wrong milestone lock and zero-tests gaps are genuinely fixed; diagnostic pathway solid. **"Completed" borderline-defensible** if order-based locking is the accepted design; not defensible if authored prerequisites are required.

---

## TASK 0007 — Build the Monaco C# workspace  *(In progress)*

> Bar: real semantic Roslyn behavior, not static helper text; Roslyn-backed + tested.

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Semantic Roslyn hover (no static placeholder) | ✅ **FIXED** | Backend resolves real `ISymbol` via semantic model, builds `ToDisplayString` (`ExerciseLanguageService.cs:243-274,639-660`); frontend `registerHoverProvider` calls endpoint (`ExerciseWorkspacePanel.tsx:383-415`); test asserts symbol info and `DoesNotContain("C# exercise workspace")` (`ExerciseEndpointTests.cs:477-511`). The forbidden static text is gone. |
| Signature help wired | ✅ **FIXED** | `registerSignatureHelpProvider` (`Panel.tsx:417-462`); `GetSignatureHelpAsync` real symbols (`:276-319`); test `:513-551`. |
| Code actions wired in Monaco | ✅ **FIXED (wiring)** | `registerCodeActionProvider` (`Panel.tsx:464-514`); Ctrl+. quick-fix (`:106-108`); tests `:553-630`. |
| Code actions are real Roslyn CodeFix/Refactoring | ❌ **STILL BROKEN** | Still hand-rolled AST rewrites (`AddExpressionBodyRefactoringAsync`, `AddThrowCompletionActionsAsync`, `AddMissingUsingActionsAsync` `:454-566`); no `CodeFixProvider`/`CodeRefactoringProvider` types exist; `WellKnownNamespaceForType` is a 5-entry hardcoded switch (`:576-587`). ~3 narrow cases vs. the real C# fix set. |
| Analyzer/style diagnostics from real analyzers | ❌ **STILL BROKEN** | `GetAnalyzerDiagnosticsAsync` reads `Project.AnalyzerReferences` (`:432`) but `CreateContextAsync` never adds any → always `[]`. The PTP0001 "analyzer" test passes via hand-rolled `GetStaticAnalysisDiagnosticsAsync` (`:110-157`), not a Roslyn `DiagnosticAnalyzer`. |
| Compiler diagnostics / completion / formatting | ✅ | Roslyn-backed + tested (completion `:159-241`, formatting `Formatter.FormatAsync :345-359`). |
| Workspace/compilation caching | ❌ **STILL BROKEN** | `CreateContextAsync` builds a fresh `AdhocWorkspace` + project + references + docs on **every** call (`:361-421`). No per-profile/exercise cache. (MSBuildWorkspace/OpenProjectAsync was dropped — that liability is gone — but static `Lazy` MEF host + warmup only reduce cold-start, not per-request rebuild.) |
| SetupMessages surfaced (was swallowed) | ✅ **FIXED** | Rendered as Monaco markers `setupMessageMarkers` (`Panel.tsx:593,606-617`); diagnostics failure now `console.warn` not silent. |
| `.editorconfig` emitted | ✅ **FIXED (cosmetic)** | `WriteFileIfMissing(... ".editorconfig" ...)` (`ExerciseWorkspaceService.cs:47,458-467`). Caveat: only PTP0001/PTP0002 severities, and NOT fed to the AdhocWorkspace as `AnalyzerConfigDocument` → no effect on live IntelliSense. |
| Tests for hover/sig-help/code-actions/analyzer | ⚠️ Partial | Real endpoint tests for hover, sig-help, code actions (`:477-630`). The "analyzer" test (`:213-244`) validates the hand-rolled PTP0001, not a Roslyn analyzer — false confidence. |

**Remaining gaps:** Code actions are hand-rolled (the exact "static helper" anti-pattern, relocated server-side); real analyzers never execute; no workspace caching (every keystroke rebuilds the workspace + reloads trusted-platform metadata — won't scale); `.editorconfig` is cosmetic; hover/sig-help only resolve symbols in the small in-memory project.

**Verdict:** 🟡 Hover, sig-help, code-action wiring, SetupMessages, and `.editorconfig` are genuinely fixed. But **two core "real Roslyn" claims remain false** (code actions hand-rolled; analyzers don't run) and there is **no caching**. "In progress" remains accurate; **not yet "Full IntelliSense / feels like the MS C# extension."**

---

## TASK 0008 — Generate real .NET exercise workspaces  *(Completed)*

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Per-attempt isolated temp workspace | ⚠️ **PARTIALLY FIXED** | Editing workspace still keyed by `profile+exercise` and reused (`GetWorkspacePath:231-234`, `WriteFileIfMissing:342-351`). But each **run** is copied to a unique `…/{ts}-{profile}-{exercise}-{guid}` dir (`CreateRunWorkspace:281-290`). Run-isolated, not workspace-isolated. |
| Real csproj + solution | ✅ **FIXED** | Two csproj `:441-491`; `.sln` now has real `Project(...)` entries (`:425-439`); test asserts both paths (`ExerciseEndpointTests.cs:23-27`). Was empty stub. |
| Starter files from content | ✅ **FIXED** | Read from `definition.Workspace.Starter` → content file (`:49-51,270-279`); `content/.../_shared/WordFrequencyAnalyzer.cs`. Was hardcoded. |
| Visible tests from content | ✅ **FIXED** | Assertion read from `exercise.yml` → generated `VisibleTests.cs` (`:53,493-510`). |
| Hidden tests available to runner, not UI | ✅ | Written to disk (`:54`); file list returns hidden with `Content=null` (`:176`); test `:30`. |
| Deterministic / idempotent per attempt | ⚠️ **REGRESSION** | `WriteFileIfMissing` means a changed `exercise.yml` (edited tests) is **never overwritten** in an existing workspace — stale tests persist. |
| Cleanup safe | ✅ **FIXED (data-loss)** | `CleanupOldWorkspacesAsync` now top-level only `Directory.GetDirectories(root)` (no `AllDirectories`), 14-day cutoff, `StartsWith(root)` guard (`:203-229`). Recursive descent gone. |

**Remaining gaps:** Editing workspace not per-attempt-isolated (concurrent same-profile sessions share a dir). **`exercise-runs` directory has NO cleanup** — every `/run` leaves a guid dir forever (a new disk-leak introduced by the run-isolation fix; cleanup only sweeps `exercise-workspaces`). Idempotency breaks on content edits.

**DATA-LOSS cleanup fixed?** **YES** — deletion scoped to top-level workspace dirs only.

**Verdict:** ⚠️ Real csproj/sln, content-sourced files, and safe cleanup fixed. Per-attempt isolation only half-true; run dirs leak; idempotency regresses on edits. **"Completed" overstated but no longer dangerous.**

---

## TASK 0009 — Run C# exercises with visible and hidden tests  *(Completed)*

| Criterion | Verdict | Evidence / status |
|---|---|---|
| Known dotnet commands, no shell strings | ✅ | `ProcessStartInfo` + `ArgumentList.Add`, `UseShellExecute=false` (`ExerciseRunner.cs:38-50`). |
| Timeout + output limits | ✅ | 20s linked-CTS + `Kill(entireProcessTree)` (`:16,33-34,76-100`); truncate 24k (`:102-105`). |
| Capture diagnostics/stdout/stderr/results | ✅ | `CommandResult` (`:60-74`). |
| Visible vs hidden WITHOUT exposing hidden source | ✅ **SECURITY FIXED** | Hidden output replaced with canned learner messages `HiddenOutputForLearner`/`HiddenDiagnosticsForLearner` (`ExerciseWorkspaceService.cs:378-418`); raw output/assertion values/literals never emitted on failure. |
| Failing-hidden-run test | ✅ **FIXED** | `RunEndpointDoesNotLeakHiddenTestDetailsWhenHiddenTestsFail` asserts `FailedHidden` + `DoesNotContain` "HiddenScenario"/"HiddenTests"/"ArgumentNullException" (`ExerciseEndpointTests.cs:81-131`). |
| Results in SQLite | ✅ | `:145-153`. |
| failed/passed/timedout/runner-error states | ✅ **FIXED** | `BuildStatus` returns all five incl. `RunnerError` (`:353-376`). |
| Runner behind abstraction | ✅ **FIXED** | `IExerciseRunner` + `DotnetExerciseRunner` (`ExerciseRunner.cs:6-13`); DI `AddScoped<IExerciseRunner, DotnetExerciseRunner>` (`Program.cs:11`). |
| RunnerError handled (was unhandled 500) | ✅ **FIXED** | `Process.Start` null → `RunnerError`; outer catch → `RunnerError` (`:54-85,121-124`), stored. |

**Remaining gaps:** No per-run cleanup of the copied run workspace (shared with 0008). `IExerciseRunner` is an abstraction but the only impl runs learner code **in-process on the host with full privileges** — no sandbox/container yet (criterion is "abstraction that *can* support containers" — met; actual isolation is not). Visible/hidden split relies on `--filter FullyQualifiedName~HiddenTests`/`~VisibleTests` — fragile naming convention (low risk since files are generated).

**SECURITY leak fixed?** **YES** — canned messages only; enforced by a dedicated failing-hidden test.

**Verdict:** ✅ Security leak, runner abstraction, and RunnerError all genuinely fixed and tested. **"Completed" now defensible**, with residual caveats (no sandbox impl; run-dir disk leak).

---

## Recommended status corrections (revised)

| Task | Current | Recommended now |
|---|---|---|
| 0001 | Completed | Completed ✅ |
| 0002 | Completed | **In progress** — add explicit `light` theme ruleset; make palette dynamic over content |
| 0003 | Completed | **In progress** — parse `exercise.yml kind`; add idempotency + broken-content tests |
| 0004 | Completed | **In progress** — real markdown lib; model review/project sections; wire real Dashboard mastery |
| 0005 | Completed | Completed-with-followup — add markdown-body search; broaden note-target tests |
| 0006 | Completed | Completed-with-followup — either parse authored prerequisites or remove the dead `.md` section |
| 0007 | In progress | In progress (accurate) — real Roslyn CodeFix/Refactoring + analyzers; add workspace caching |
| 0008 | Completed | **In progress** — clean up `exercise-runs`; refresh stale files on content change; per-attempt isolation |
| 0009 | Completed | Completed ✅ (add run-dir cleanup; sandbox is future work) |

### New / highest-priority follow-ups (in order)

1. **0008 `exercise-runs` disk leak** — the run-isolation fix introduced an unbounded directory that nothing cleans up. Add cleanup for the run root.
2. **0007 real Roslyn analyzers + CodeFix/CodeRefactoring providers** — the headline "Full IntelliSense" bar still fails; the hand-rolled path is the forbidden anti-pattern relocated to the server.
3. **0007 workspace caching** — per-keystroke `AdhocWorkspace` rebuild won't scale.
4. **0003 `exercise.yml kind` + negative tests; 0004 markdown lib + Dashboard mastery** — the two tasks that did not move at all.
5. **0008 idempotency-on-edit** — `WriteFileIfMissing` silently ignores updated content.

### Resolved (verified) — no longer concerns

- ✅ **0009 hidden-test source leak (security)** — fixed + failing-hidden test.
- ✅ **0008 recursive cleanup (data-loss)** — fixed, top-level only.
- ✅ **0002 missing keyboard shortcuts, non-per-profile theme, zero frontend tests** — all fixed.
- ✅ **0005 concept notes/page, weak deep-links, in-memory search** — fixed (search → real SQL).
- ✅ **0006 semantically-wrong milestone soft-lock, zero soft-lock tests** — fixed.
- ✅ **0007 forbidden static hover, unwired hover/sig-help/code-actions, swallowed SetupMessages** — fixed.

def build_note_label(title: str, body: str) -> str:
    """Return a short display label for a note.

    Contract:
    - trim whitespace around title and body
    - return "<title>: <body>"
    - do not print
    """
    return ""


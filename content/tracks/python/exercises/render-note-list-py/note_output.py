def render_note_list(notes: list[dict[str, object]]) -> str:
    """Render trusted note records for py-notes list output.

    Contract:
    - return "No notes." when the list is empty
    - render one numbered line per note
    - each line contains the title and comma-separated tags
    - do not print; return the output string
    """
    return ""

def normalize_word(word: str) -> str:
    """Normalize one word for matching."""
    return word


def normalize_words(words: list[str]) -> list[str]:
    """Normalize many words, dropping empty results."""
    return words

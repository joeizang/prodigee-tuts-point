def parse_database_url(url: str) -> dict[str, object]:
    """Parse and validate a SQLite or PostgreSQL database URL."""
    return {}


def portability_warnings(sql_snippets: list[str]) -> list[str]:
    """Return warnings for SQL snippets that are not PostgreSQL-ready."""
    return []

def build_note_draft(title: str, body: str, tags: list[str]) -> dict[str, object]:
    """Return a note-shaped dictionary from beginner-safe inputs.

    Contract:
    - trim title and body
    - reject blank title or body
    - copy tags into the record
    - return keys: title, body, tags
    """
    return {}

